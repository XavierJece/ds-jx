{
 "private": true,
 "workspaces": [
  "packages/*"
 ],
 "scripts": {
  "dev": "turbo run dev --parallel",
  "build": "turbo run build"
 },
 "dependencies": {
  "@storybook/source-loader": "^7.0.18"
 },
 "devDependencies": {
  "turbo": "^1.10.1"
 }
}
